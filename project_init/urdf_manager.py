"""
Given official xacro files, this manager python script generates the combined URDF file 
and saves it to the specified location (src/robot_gazebo/models/robots/example.urdf).
This will be implemented later(for the scalability of the project, we should, but not 
necessary for the current scope).
"""
